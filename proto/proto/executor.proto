syntax = "proto3";

import "blockchain.proto";
import "common.proto";

package executor;

// The error code in result should be ReceiptError
message ExecutionResp {
  common.Result result = 1;
  blockchain.Receipt receipt = 2;
}

service ExecutorService {
  rpc Apply(blockchain.SignedTransaction) returns (ExecutionResp);
}
