buildPack: none
pipelineConfig:
  pipelines:
    pullRequest:
      pipeline:
        agent:
          image: mutadev/muta-build-env:v0.1.0
        stages:
          - name: unit
            parallel:
              - name: "cargo test"
                steps:
                  - command: make
                    args:
                      - test
