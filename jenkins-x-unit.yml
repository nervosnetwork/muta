buildPack: none
pipelineConfig:
  pipelines:
    pullRequest:
      pipeline:
        agent:
          image: mutadev/muta:build-env
        stages:
          - name: unit
            parallel:
              - name: "cargo test"
                steps:
                  - command: make
                    args:
                      - test
