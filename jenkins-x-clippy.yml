buildPack: none
pipelineConfig:
  pipelines:
    pullRequest:
      pipeline:
        agent:
          image: mutadev/muta:build-env
        stages:
          - name: "clippy"
            parallel:
              - name: "cargo fmt"
                steps:
                  - command: make
                    args:
                      - clippy
