buildPack: none
pipelineConfig:
  pipelines:
    pullRequest:
      pipeline:
        agent:
          image: mutadev/muta-e2e-env:v0.1.0
        stages:
          - name: e2e
            parallel:
              - name: "cargo e2e"
                steps:
                  - command: make
                    args:
                      - e2e-test
